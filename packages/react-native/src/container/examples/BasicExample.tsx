import { Typography } from "../../typography";
import { Container } from "../Container";

export const BasicExample = () => {
  return (
    <Container>
      <Typography variant="body">
        Content centered with default lg max-width (1024px) and 16px horizontal
        padding.
      </Typography>
    </Container>
  );
};
